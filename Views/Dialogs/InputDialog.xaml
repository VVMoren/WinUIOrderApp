<Window x:Class="WinUIOrderApp.Views.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="150" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="Поле:" VerticalAlignment="Center" Width="60"/>
            <ComboBox x:Name="FieldCombo" Width="140" SelectionChanged="FieldCombo_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="Значение:" VerticalAlignment="Center" Width="60"/>
            <!-- для name/inn -->
            <TextBox x:Name="ValueBox" Width="240" Visibility="Collapsed" TextChanged="ValueBox_TextChanged"/>

            <!-- для create/ip -->
            <ComboBox x:Name="ValueCombo" Width="240" Visibility="Collapsed" IsEditable="True" SelectionChanged="ValueCombo_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="4" Click="OkButton_Click" IsEnabled="False"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Margin="4" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
